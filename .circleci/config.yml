version: '2.1'
orbs:
  node: circleci/node@5.0.0
jobs:
  test:
    machine: true
    steps:
      - checkout
      - node/test:
          test-results-for: jest
workflows:
  test_my_app:
    jobs:
      - test