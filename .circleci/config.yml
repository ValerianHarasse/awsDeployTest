version: 2.1

orbs:
  eb: circleci/aws-elastic-beanstalk@2.0.1
  node: circleci/node@5.0.0

jobs:
  checkoutAndTest:
    machine: true
    steps:
      - checkout
      - node/test

workflows:
  monWorkflow:
    jobs:
      - checkoutAndTest
